// install ripgrep for telescope live-grep
cargo install ripgrep

// install python provider
pip install pynvim

// if will use jdtls lsp server, install like this:
:LspInstall jdtls@1.12.0-202206011637

// lldb for debugging c, cpp and rust
yay -S lldb

// netcoredbg for debugging dotnet core
https://github.com/Samsung/netcoredbg/releases

// cmake can be installed with visual studio on windows

// llvm (for clang)
choco install llvm

// fzf
choco install fzf
choco install mingw
choco install make

// really nice font
CascaydiaCove nerd font
